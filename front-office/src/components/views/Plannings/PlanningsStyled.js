import styled from 'styled-components';
import theme from '../../../theme/theme';

const PlanningsStyled = styled.div`
  h2.title, p.sub-txt {
    margin: 1em 5em;
  }


`;

export default PlanningsStyled;