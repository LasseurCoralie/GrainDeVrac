{% extends 'pages/backOffice/templateBackOffice.html.twig' %}

{% block body %}
    <div class="main">

        <a href="{{ path('calendar.newDate') }}"><button>Ajouter une nouvelle date</button></a>

        {% for data in datas %}
            <div class="formCard">
                <h2>{{data.title}}</h2>
                <div class="content">{{data.content}}</div>
                <a href="{{ path('home.homepage.edit', {id: data.id}) }}"><button>editer</button><a/>
            </div>
        {% endfor %}
    </div>
{% endblock %}